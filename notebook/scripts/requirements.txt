# =============================================================================
# scripts/train.py 用の依存関係（SageMaker 学習ジョブコンテナ内で pip install される）
# =============================================================================
# ModelTrainer の SourceCode(requirements="requirements.txt") で指定する。
# 組み込み XGBoost コンテナには xgboost 自体は既に入っているが、
# MLflow へのロギングと評価指標の計算に必要なパッケージを追加でインストールする。
# =============================================================================
mlflow<4
sagemaker-mlflow>=0.5.0,<1
scikit-learn==1.4.2
pandas>=2,<3
# System metrics（CPU/メモリ使用率など）を MLflow に記録するために必要
psutil>=5,<7
